{
  "name": "@capire/incidents",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": ">=8",
    "@cap-js/change-tracking": "git+https://github.com/stefanrudi/change-tracking#feat/hana-trigger",
    "express": "^4",
    "@cap-js/hana": "^2",
    "@sap/xssec": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-test": "*",
    "@cap-js/sqlite": ">=1",
    "@sap/cds-dk": ">=8"
  },
  "scripts": {
    "watch": "cds watch",
    "start": "cds-serve",
    "jest": "npx jest --silent",
    "test": "chest"
  },
  "sapux": [
    "app/incidents"
  ],
  "private": true,
  "cds": {
    "requires": {
      "[production]": {
        "auth": "xsuaa"
      },
      "html5-runtime": true,
      "portal": true
    }
  }
}
